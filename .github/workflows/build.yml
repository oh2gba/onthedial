name: build

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and test in Docker
        run: ./build.sh

  linux-appimage:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - run: ./package-linux.sh
      - uses: actions/upload-artifact@v4
        with:
          name: otd-linux-appimage
          path: dist/*.AppImage

  windows:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - run: ./package-windows.sh
      - uses: actions/upload-artifact@v4
        with:
          name: otd-windows-x64
          path: dist/*.zip

  macos:
    runs-on: macos-14
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Install Qt
        run: |
          brew install ninja
          pip3 install --break-system-packages aqtinstall
          aqt install-qt --outputdir "$HOME/qt" mac desktop 6.8.3 clang_64 --archives qtbase
      - name: Build universal app bundle
        run: |
          QT="$HOME/qt/6.8.3/macos"
          VERSION=$(sed -n 's/^project(onthedial VERSION \([0-9.]*\).*/\1/p' CMakeLists.txt)
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_PREFIX_PATH="$QT" -DOTD_BUILD_TESTS=OFF \
                -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" -DCMAKE_OSX_DEPLOYMENT_TARGET=12.0
          cmake --build build
          "$QT/bin/macdeployqt" build/otd.app -dmg
          mkdir -p dist && mv build/otd.dmg "dist/otd-$VERSION-macos.dmg"
      - uses: actions/upload-artifact@v4
        with:
          name: otd-macos
          path: dist/*.dmg

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    needs: [linux-appimage, windows, macos]
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: Version-free file names, so /releases/latest/download/ links stay valid
        run: |
          cd dist
          for f in otd-*; do mv "$f" "$(echo "$f" | sed -E 's/^otd-[0-9.]+-/otd-/')"; done
          sha256sum * > SHA256SUMS.txt
          ls -la
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
