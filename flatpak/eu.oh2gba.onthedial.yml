# Flatpak manifest for On The Dial. Local build: flatpak/build.sh
# For Flathub the "sources" of the last module point at a release tag.
app-id: eu.oh2gba.onthedial
runtime: org.kde.Platform
runtime-version: '6.11'
sdk: org.kde.Sdk
command: otd
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  # Schedule downloads and the TCP connection to rigctld
  - --share=network
  # Serial/USB access so rigctld started from the app can reach the radio
  - --device=all
cleanup:
  - /include
  - /lib/pkgconfig
  - /share/man
  - /share/aclocal
  - '*.la'
  - '*.a'
modules:
  - name: hamlib
    buildsystem: autotools
    config-opts:
      - --disable-static
      - --without-cxx-binding
      - --without-perl-binding
      - --without-python-binding
      - --without-tcl-binding
      - --without-lua-binding
    cleanup:
      - /bin/ampctl*
      - /bin/rigmem
      - /bin/rigsmtr
      - /bin/rigswr
      - /bin/rotctl*
      - /bin/rigtest*
    post-install:
      - install -Dm644 COPYING.LIB ${FLATPAK_DEST}/share/licenses/${FLATPAK_ID}/hamlib/COPYING.LIB
    sources:
      - type: archive
        url: https://github.com/Hamlib/Hamlib/releases/download/4.7.2/hamlib-4.7.2.tar.gz
        sha256: ae1fcf2dbc80ea0786ea8f047b09399c3f7737d1930442f61a031708ed33e88f

  - name: onthedial
    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DOTD_BUILD_TESTS=OFF
      - -DOTD_DESKTOP_ID=eu.oh2gba.onthedial
    post-install:
      - install -Dm644 LICENSE ${FLATPAK_DEST}/share/licenses/${FLATPAK_ID}/onthedial/LICENSE
      - install -Dm644 third_party/miniz/LICENSE ${FLATPAK_DEST}/share/licenses/${FLATPAK_ID}/miniz/LICENSE
    sources:
      - type: git
        url: https://github.com/oh2gba/onthedial.git
        tag: v1.4.1
        commit: c07be3c4d3bcdc0195210ab9294372f47b66ad29
